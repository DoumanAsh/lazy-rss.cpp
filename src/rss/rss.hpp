#pragma once

#include "parser.hpp"
#include "item.hpp"

/**
 * RSS Parser module.
 *
 * ## List of sub-modules:
 * * @ref parser;
 * * @ref item;
 */
namespace rss {
    namespace parser = parser;
    namespace item = item;
}
